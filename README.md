# Probo

Prototype Android network utility featuring a Compose-based UI and an extensible navigation drawer for diagnostics tools.

## Building

This repository omits the binary `gradle-wrapper.jar` so it can be shared in environments that disallow binary attachments. The provided `gradlew` script automatically downloads the matching wrapper jar on first use (requires `curl` or `wget`).
